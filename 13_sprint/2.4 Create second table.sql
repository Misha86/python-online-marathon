CREATE TABLE ORDERS (ID INT PRIMARY KEY,
                     PRODUCT_NAME VARCHAR(30),
                     PRODUCT_PRICE FLOAT,
                     DATE_ORDER DATE,
                     ID_CUSTOMER INT);